{
  "name": "@cumjar/cummunicate",
  "version": "1.0.1",
  "description": "Cummunicate with the CC websocket in a sane way",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "prepublish": "rm -rf dist && tsc && esbuild src/browser.js --bundle --format=esm --outfile=dist/browser.js"
  },
  "repository": "github:cumcord/cummunicate",
  "author": "Cumcord Team",
  "license": "CC-0",
  "devDependencies": {
    "esbuild": "^0.14.39",
    "typescript": "^4.6.4"
  }
}
